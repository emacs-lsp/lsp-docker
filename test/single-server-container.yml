# support to test single container-based server
lsp:
  server:
    type: docker
    subtype: container
    name: "test-pylspService-1"
    server: pylsp
  mappings:
    - source: "."
      destination: "/code"
