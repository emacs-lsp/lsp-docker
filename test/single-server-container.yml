lsp:
  server:
    type: docker
    subtype: container
    name: "test-pylspService-1"
    server: "pylsp"
    # if the same nothing bad happens
  mappings:
    - source: "."
      destination: "/code"
